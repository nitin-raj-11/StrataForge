# URL of the Spring Boot backend's API root
VITE_API_URL=http://localhost:8080/api

# true  -> use the built-in in-browser mock engine (no backend required)
# false -> call the real backend at VITE_API_URL
VITE_USE_MOCK_API=true
